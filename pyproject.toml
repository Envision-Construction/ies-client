[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ies-client"
version = "0.1.0"
description = "Shared Intuit Enterprise Suite (QuickBooks Online) OAuth token client for Envision GCP services"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
